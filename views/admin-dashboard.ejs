<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Quản trị người dùng</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <a href="/logout" class="btn-logout-fixed">⎋ Đăng xuất</a>

  <div class="container">
    <div class="page-header">
      <h1>Quản lý tài khoản người dùng</h1>
      <a href="/" style="text-decoration: none; color: #007bff; font-weight: bold;">← Về trang nhập đơn</a>
    </div>

    <% if (locals.success_msg) { %>
      <div ><%= success_msg %></div>
    <% } %>
    <% if (locals.error_msg) { %>
      <div ><%= error_msg %></div>
    <% } %>

    <div style="background: #f9f9f9; padding: 20px; border-radius: 8px; margin-bottom: 30px;">
        <h2 style="margin-top: 0;">Thêm người dùng mới</h2>
        <form method="POST" action="/admin/add">
          <div style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 10px; align-items: end;">
            <div>
                <label style="display:block; font-size: 12px;">Tên đăng nhập</label>
                <input name="username" required style="width:100%; padding: 8px; border: 1px solid #ccc; border-radius: 4px;" />
            </div>
            <div>
                <label style="display:block; font-size: 12px;">Mật khẩu</label>
                <input type="password" name="password" required style="width:100%; padding: 8px; border: 1px solid #ccc; border-radius: 4px;" />
            </div>
            <div>
                <label style="display:block; font-size: 12px;">Họ tên</label>
                <input name="fullName" required style="width:100%; padding: 8px; border: 1px solid #ccc; border-radius: 4px;" />
            </div>
            <button type="submit" class="btn-add" style="height: 35px;">Thêm</button>
          </div>
        </form>
    </div>

    <h2>Danh sách người dùng</h2>
    <div class="admin-table-container">
      <table class="admin-table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Tên đăng nhập</th>
            <th>Tên hiển thị</th>
            <th>Vai trò</th>
            <th style="text-align: center;">Hành động</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(u => { %>
            <tr>
              <td><%= u.id %></td>
              <td><strong><%= u.username %></strong></td>
              <td><%= u.fullName %></td>
              <td>
                <span style="padding: 2px 8px; border-radius: 12px; font-size: 12px; background: <%= u.role === 'admin' ? '#fff7e6' : '#e6f7ff' %>; color: <%= u.role === 'admin' ? '#fa8c16' : '#1890ff' %>; border: 1px solid <%= u.role === 'admin' ? '#ffd591' : '#91d5ff' %>;">
                    <%= u.role === 'admin' ? 'Quản trị' : 'Nhân viên' %>
                </span>
              </td>
              <td style="text-align: center;">
                <% if (u.username !== 'admin') { %> 
                  <form method="POST" action="/admin/delete/<%= u.id %>" style="display:inline">
                    <button type="submit" class="btn-delete" onclick="return confirm('Bạn có chắc chắn muốn xóa tài khoản này?')">Xóa</button>
                  </form>
                <% } else { %>
                  <small style="color: #ccc;">Mặc định</small>
                <% } %>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>